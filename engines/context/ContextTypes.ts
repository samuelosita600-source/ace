export interface Context {
      userId: string;
        message: string;
          conversationId?: string;
            memories?: unknown[];
              goals?: unknown[];
                profile?: unknown;
                  timestamp: Date;
                  }
